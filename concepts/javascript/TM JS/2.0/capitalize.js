// const myStr = 'developer';
// // This line capitalizes the first letter of the string 'myStr':
// // 1. myStr.charAt(0) selects the first character of the string
// // 2. .toUpperCase() converts that character to uppercase
// // 3. myStr.slice(1) returns the rest of the string starting from the second character
// // 4. The '+' operator concatenates the capitalized first letter with the rest of the string
// // The result is stored in the 'capitalized' variable
// const capitalized = myStr.charAt(0).toUpperCase() + myStr.slice(1);
// console.log(capitalized); // Output: Developer

// let amount = 'hello';
// // change string to number
// amount = parseInt(amount);

// console.log(amount);

// amount = +amount;
// amount = Number(amount);
// console.log(amount, typeof amount);

// // change number to string
// amount = amount.toString();
// amount = String(amount);

// // change string to decimal
// amount = parseFloat(amount);

// // convert number to boolean
// amount = Boolean(amount);



// console.log(amount, typeof amount)

console.log(Math.sqrt(-1))
console.log(undefined + undefined)